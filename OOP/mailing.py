
from address import Address
class Mailing:

    def __init__(self, self.Address., from_address, cost, track):
        self.to_address = to_address
        self.from_address = from_address
        self.cost = cost
        self.track = track

        print('Address entered')

    def __str__(self):
        return f'Отправление {self.track} из {self.index}. Цена {self.cost}. Track-number {self.track}'

# new = Mailing('Moscow', 'New-York', '10', '12345678')
# print(f'{new}')